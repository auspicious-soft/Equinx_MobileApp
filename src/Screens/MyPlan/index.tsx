import { StyleSheet, Text, View } from 'react-native'
import React, { FC } from 'react'
import { MyPlanScreenProps } from '../../Typings/route'

const MyPlan: FC <MyPlanScreenProps> = () => {
  return (
    <View>
      <Text>MyPlan</Text>
    </View>
  )
}

export default MyPlan

const styles = StyleSheet.create({})